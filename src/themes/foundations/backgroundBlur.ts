import colors from './colors';
import radius from './radius';

const backgroundBlur = {
  colors: {
    light: `${colors.base.articWindWhite}80`,
    dark: `${colors.base.polarNightBlack}80`,
  },

  blurRadius: {
    md: radius.md,
  },
};

export default backgroundBlur;
